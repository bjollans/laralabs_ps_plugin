import React from "react";

export const LaraPrompt = (props) => {
    return (
        <>
            <sp-field-label for="prompt-0">Description (Prompt)</sp-field-label>
            <sp-textfield id="prompt-0" multiline placeholder="What to create"/>
        </>
    );
} 
